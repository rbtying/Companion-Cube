#include <ros/ros.h>
#include "wrapper.h"

int main(int argc, char** argv) {
	ros::init(argc, argv, "rover_node");
	ROS_INFO("started");
	Wrapper rover;
	ROS_INFO("constructed wrapper, spinning");
	ros::spin();
	ROS_INFO("shouldn't get here");
}
