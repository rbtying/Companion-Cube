/*
 * servomapping.h
 *
 *  Created on: Jan 17, 2012
 *      Author: rbtying
 */

#ifndef SERVOMAPPING_H_
#define SERVOMAPPING_H_

#include <string>

namespace servomapping {

#define NUM_SERVOS_IN_MAPPING 2
std::string mapping[] = { "panservo", "tiltservo" };
std::string servo;

std::string servoname(uint8_t i) {
	if (i < NUM_SERVOS_IN_MAPPING) {
		return mapping[i];
	} else {
		return servo;
	}
}

uint8_t servoid(std::string name) {
if (name.compare(pan) == 0)
}
}

#endif /* SERVOMAPPING_H_ */
